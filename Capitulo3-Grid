from tkinter import *

root = Tk()

Label(root, text="Fila 1").grid(row=0, column=0)
Label(root, text="Fila 2").grid(row=1, column=0)
Label(root, text="Fila 3").grid(row=2, column=0)

root.mainloop()